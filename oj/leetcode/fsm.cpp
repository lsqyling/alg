#include "../utilities.hpp"


/**
 * 有限状态自动机（Finite State Machine，简称 FSM），
 * 也称为有限状态机或有限自动机（Finite Automaton, FA），
 * 是计算机科学中用于建模系统行为的一种数学计算模型。它在编译原理、正则表达式、协议设计、游戏 AI、词法分析等领域有广泛应用。
 *
 * 什么是有限状态自动机？
 * 它是一个抽象机器，在任意时刻处于有限个状态中的某一个。
 * 根据输入和当前状态，按照转移规则跳转到下一个状态。
 * 状态数量是有限的（故名“有限”）。 ✅ 核心思想：“状态 + 输入 → 新状态”
 */

namespace leetcode_65 {
// 65. 有效数字
class Solution {
public:
    bool isNumber(std::string s) {
    }
};
}



























int main() {
    return 0;
}